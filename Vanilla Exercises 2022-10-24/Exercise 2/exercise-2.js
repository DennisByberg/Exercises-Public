// Create an array that contains 3 different data types, e.g. string, number, array.
const myWeirdArray = ["Tree", 3, [1, 2, 3]];

// Make sure the length is 3 to make sure we did it right.
// 1 - string
// 2 - number
// 3 - array
console.log(myWeirdArray);
console.log(`The length of the array is: ${myWeirdArray.length}`);
